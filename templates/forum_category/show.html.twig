{% extends 'base.html.twig' %}

{% block title %}ForumCategory{% endblock %}

{% block body %}
    <h1>ForumCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ forum_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ forum_category.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ forum_category.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_forum_category_index') }}">back to list</a>

    <a href="{{ path('app_forum_category_edit', {'id': forum_category.id}) }}">edit</a>

    {{ include('forum_category/_delete_form.html.twig') }}
{% endblock %}
