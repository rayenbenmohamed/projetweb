{% extends 'layout/admin.html.twig' %}

{% block title %}Candidatures | SyfonuRH{% endblock %}
{% block page_title %}Gestion des candidatures{% endblock %}

{% block content %}
<div class="row g-6 mt-2">
    <div class="col-12">
        <div class="card h-100">
            <div class="card-header border-bottom-0">
                <h5 class="mb-0">Toutes les candidatures</h5>
            </div>
            <div class="table-responsive">
                <table class="table table-hover table-centered mb-0 text-nowrap">
                    <thead class="table-light">
                        <tr>
                            <th>Candidat</th>
                            <th>Offre</th>
                            <th>Date</th>
                            <th>Statut</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for app in job_applications %}
                        <tr>
                            <td>
                                <div class="d-flex align-items-center gap-3">
                                    <div class="avatar avatar-sm rounded-circle bg-secondary-subtle">
                                        <i class="ti ti-user fs-4"></i>
                                    </div>
                                    <div>
                                        <h6 class="mb-0">{{ app.candidat.firstName }} {{ app.candidat.lastName }}</h6>
                                        <span class="small text-secondary">{{ app.candidat.email }}</span>
                                    </div>
                                </div>
                            </td>
                            <td>{{ app.jobOffre.title }}</td>
                            <td>{{ app.applyDate|date('d/m/Y') }}</td>
                            <td>
                                <span class="badge bg-{{ app.status == 'ACCEPTED' ? 'success' : (app.status == 'REJECTED' ? 'danger' : 'info') }}-subtle text-{{ app.status == 'ACCEPTED' ? 'success' : (app.status == 'REJECTED' ? 'danger' : 'info') }}-emphasis">
                                    {{ app.status }}
                                </span>
                            </td>
                            <td>
                                <form method="post" action="{{ path('app_job_application_status', {id: app.id}) }}" class="d-flex gap-2">
                                    <button type="submit" name="status" value="ACCEPTED" class="btn btn-ghost btn-icon btn-sm rounded-circle text-success" title="Accepter">
                                        <i class="ti ti-check fs-4"></i>
                                    </button>
                                    <button type="submit" name="status" value="REJECTED" class="btn btn-ghost btn-icon btn-sm rounded-circle text-danger" title="Refuser">
                                        <i class="ti ti-x fs-4"></i>
                                    </button>
                                </form>
                            </td>
                        </tr>
                        {% else %}
                        <tr>
                            <td colspan="5" class="text-center py-8">
                                <p class="text-secondary mb-0">Aucune candidature pour le moment.</p>
                            </td>
                        </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
{% endblock %}
